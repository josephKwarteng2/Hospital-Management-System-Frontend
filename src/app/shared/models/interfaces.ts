export interface UserRegistration {
  firstName: string;
  lastName: string;
  email: string;
  password: string;
}

export interface NavLink {
  routeLink: string;
  icon: string;
  label: string;
}

export interface FAQ {
  question: string;
  answer: string;
}
